<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <template id="report_simple_barcode" inherit_id="product.report_simple_barcode">
        <xpath expr="//div" position="replace">
            <t t-if="product.multi_barcode">
                <t t-foreach="product.barcode_line_ids" t-as="barcode_line">
                    <div style="width: 32%; display: inline-table; height: 10rem;">
                        <table class="table table-bordered mb-0" style="border: 2px solid black;">
                            <tr>
                                <th class="table-active text-left" style="height: 4rem;">
                                    <strong t-field="product.display_name"/>
                                    (<strong t-field="barcode_line.uom_id.name"/>)
                                </th>
                            </tr>
                            <tr>
                                <td class="text-center align-middle" style="height: 6rem;">
                                    <img alt="Barcode" t-if="len(barcode_line.name) == 13" t-att-src="'/report/barcode/?type=%s&amp;value=%s&amp;width=%s&amp;height=%s' % ('EAN13', quote_plus(barcode_line.name or ''), 600, 150)" style="width:100%;height:4rem;"/>
                                    <img alt="Barcode" t-elif="len(barcode_line.name) == 8" t-att-src="'/report/barcode/?type=%s&amp;value=%s&amp;width=%s&amp;height=%s' % ('EAN8', quote_plus(barcode_line.name or ''), 600, 150)" style="width:100%;height:4rem;"/>
                                    <img alt="Barcode" t-else="" t-att-src="'/report/barcode/?type=%s&amp;value=%s&amp;width=%s&amp;height=%s' % ('Code128', quote_plus(barcode_line.name or ''), 600, 150)" style="width:100%;height:4rem"/>
                                    <span t-field="barcode_line.name"/>
                                </td>
                            </tr>
                        </table>
                    </div>
                </t>
            </t>
            <t t-else="">
                <div style="width: 32%; display: inline-table; height: 10rem;">
                    <table class="table table-bordered mb-0" style="border: 2px solid black;">
                        <tr>
                            <th class="table-active text-left" style="height: 4rem;">
                                <strong t-field="product.display_name"/>
                            </th>
                        </tr>
                        <tr>
                            <td class="text-center align-middle" style="height: 6rem;">
                                <t t-if="product.barcode">
                                    <img alt="Barcode" t-if="len(product.barcode) == 13" t-att-src="'/report/barcode/?type=%s&amp;value=%s&amp;width=%s&amp;height=%s' % ('EAN13', quote_plus(product.barcode or ''), 600, 150)" style="width:100%;height:4rem;"/>
                                    <img alt="Barcode" t-elif="len(product.barcode) == 8" t-att-src="'/report/barcode/?type=%s&amp;value=%s&amp;width=%s&amp;height=%s' % ('EAN8', quote_plus(product.barcode or ''), 600, 150)" style="width:100%;height:4rem;"/>
                                    <img alt="Barcode" t-else="" t-att-src="'/report/barcode/?type=%s&amp;value=%s&amp;width=%s&amp;height=%s' % ('Code128', quote_plus(product.barcode or ''), 600, 150)" style="width:100%;height:4rem"/>
                                    <span t-field="product.barcode"/>
                                </t>
                                <t t-else=""><span class="text-muted">No barcode available</span></t>
                            </td>
                        </tr>
                    </table>
                </div>
            </t>
        </xpath>
    </template>

</odoo>