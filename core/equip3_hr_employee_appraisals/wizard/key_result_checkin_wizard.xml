<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="hr_key_result_checkin_wizard_form" model="ir.ui.view">
            <field name="name">hr.key.result.checkin.wizard.form</field>
            <field name="model">hr.key.result.checkin.wizard</field>
            <field name="arch" type="xml">
                <form string="Checkin">
                    <sheet>
                        <group>
                            <field name="goal_title_id"/>
                            <field name="key_result_id" invisible="1"/>
                            <field name="key_result_type" invisible="1"/>
                            <field name="milestone_template_id" invisible="1"/>
                            <field name="milestone_id" attrs="{'invisible':[('key_result_type','!=','milestone')],'required':[('key_result_type','=','milestone')]}"/>
                            <field name="key_result_target"/>
                            <field name="actual"/>
                            <field name="achievement"/>
                            <field name="attachment_file" filename="attachment_name" widget="binary"/>
                            <field name="attachment_name" invisible="1"/>
                            <field name="priority"/>
                            <field name="state"/>
                            <field name="notes"/>
                        </group>
                    </sheet>
                    <footer>
                        <button name="action_save" type="object" string="Save" class="oe_highlight"/>
                        <button special="cancel" string="Discard" confirm="Are you sure want to discard the record?"/>
                    </footer>
                </form>
            </field>
        </record>

        <record id="hr_key_result_checkin_wizard_action" model="ir.actions.act_window">
            <field name="name">Checkin</field>
            <field name="res_model">hr.key.result.checkin.wizard</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
        </record>
    </data>
</odoo>
