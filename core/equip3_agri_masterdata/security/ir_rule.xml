<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="agriculture_worker_group" model="ir.rule">
        <field name="name">Agriculture Worker Group</field>
        <field name="model_id" ref="model_agriculture_worker_group"/>
        <field name="domain_force">[
            '|',
                ('company_id', '=', False),
                ('company_id', 'in', company_ids)
        ]</field>
    </record>

</odoo>