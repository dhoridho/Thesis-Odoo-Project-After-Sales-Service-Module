<?xml version='1.0' encoding='utf-8'?>
<odoo>
    <data noupdate="1">
		<!-- <record forcecreate="True" id="expiry_document_notify_scheduler_ir_cron" model="ir.cron">
            <field name="name">Expiry Documents Notification</field>
            <field name="model_id" ref="model_maintenance_equipment"/>
            <field name="state">code</field>
            <field name="code">model.send_notification_to_user()</field>
            <field name="user_id" ref="base.user_root" />
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="nextcall" eval="datetime.now() + (datetime.now(pytz.timezone('Asia/Singapore')).replace(hour=2, minute=17, second=00) - datetime.now(pytz.timezone('Asia/Singapore'))) % timedelta(hours=24)"/>
            <field name="numbercall">-1</field>
            <field eval="True" name="doall" />
        </record> -->
    </data>
</odoo>