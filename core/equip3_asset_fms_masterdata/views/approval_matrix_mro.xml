<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="approval_matrix_mro_view_tree" model="ir.ui.view">
        <field name="name">approval.matrix.mro.view.tree</field>
        <field name="model">approval.matrix.mro</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name" />
                <field name="company_id" />
                <field name="state" string="Stages"/>
                <field name="branch_id" />
                <field name="min_amount" />
                <field name="max_amount" />
            </tree>
        </field>
    </record>

    <record id="approval_matrix_mro_view_form" model="ir.ui.view">
        <field name="name">approval.matrix.mro.view.form</field>
        <field name="model">approval.matrix.mro</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="company_id" />
                            <field name="branch_id" />
                            <field name="state" string="Stages"/>
                        </group>
                        <group>
                            <field name="created_date"/>
                            <field name="user_id"/>
                            <field name="min_amount"/>
                            <field name="max_amount"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Approval">
                            <field name="approval_matrix_mro_ids">
                                <tree editable="bottom">
                                    <field name="user_id" string="User"  widget="many2many_tags"/>
                                    <field name="min_approvers" string="Minimum Approvers"/>
                                </tree>
                            </field>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids" widget="mail_followers" />
                    <field name="activity_ids" widget="mail_activity" />
                    <field name="message_ids" widget="mail_thread" />
                </div>
            </form>
        </field>
    </record>

    <record id="approval_matrix_mro_action" model="ir.actions.act_window">
        <field name="name">Maintenance Repair Order Approval Matrix</field>
        <field name="res_model">approval.matrix.mro</field>
        <field name="type">ir.actions.act_window</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p></p>
        </field>
    </record>

    <menuitem id="menu_approval_matrix_mro" name="Maintenance Repair Order Approval Matrix" parent="menu_approval_matrix" action="approval_matrix_mro_action"/>

</odoo>
