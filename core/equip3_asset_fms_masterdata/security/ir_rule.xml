<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <data>
        <record id="maintenance_facilities_area_multi_branch_rule" model="ir.rule">
            <field name="name">Maintenance Facilities Area Multi-Branch</field>
            <field name="model_id" ref="model_maintenance_facilities_area"/>
            <field name="domain_force">['|',
                                    ('branch', '=', False),
                                    ('branch', 'in', branch_ids)
                                    ]
            </field>
        </record>

        <record id="approval_matrix_mmwo_multi_branch_rule" model="ir.rule">
            <field name="name">Approval Matrix MWO Multi-Branch</field>
            <field name="model_id" ref="model_approval_matrix_mwo"/>
            <field name="domain_force">['|',
                                    ('branch_id', '=', False),
                                    ('branch_id', 'in', branch_ids)
                                    ]
            </field>
        </record>

        <record id="approval_matrix_mro_multi_branch_rule" model="ir.rule">
            <field name="name">Approval Matrix MRO Multi-Branch</field>
            <field name="model_id" ref="model_approval_matrix_mro"/>
            <field name="domain_force">['|',
                                    ('branch_id', '=', False),
                                    ('branch_id', 'in', branch_ids)
                                    ]
            </field>
        </record>

        <record id="approval_matrix_mp_multi_branch_rule" model="ir.rule">
            <field name="name">Approval Matrix MP Multi-Branch</field>
            <field name="model_id" ref="model_approval_matrix_mp"/>
            <field name="domain_force">['|',
                                    ('branch_id', '=', False),
                                    ('branch_id', 'in', branch_ids)
                                    ]
            </field>
        </record>

        <record id="approval_matrix_asset_transfer_multi_branch_rule" model="ir.rule">
            <field name="name">Approval Matrix Asset Transfer Multi-Branch</field>
            <field name="model_id" ref="model_approval_matrix_asset_transfer"/>
            <field name="domain_force">['|',
                                    ('branch_id', '=', False),
                                    ('branch_id', 'in', branch_ids)
                                    ]
            </field>
        </record>
    </data>
    

</odoo>
