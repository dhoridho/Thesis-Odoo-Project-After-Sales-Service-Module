<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_account_payment_form" model="ir.ui.view">
        <field name="name">account.payment.form</field>
        <field name="model">account.payment</field>
        <field name="inherit_id" ref="equip3_accounting_operation.mr_account_payment_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='create_date']" position="after">
                <field name="online_payment_status" attrs="{'invisible': [('show_partner_bank_account', '=', False)]}"/>
            </xpath>

            <button name="action_draft" position="attributes">
                <attribute name="attrs">{'invisible': [('state', 'not in', ('posted', 'cancel', 'failed'))]}</attribute>
            </button>

            <button name="action_cancel" position="attributes">
                <attribute name="attrs">{'invisible': [('state', 'not in', ('draft', 'failed'))]}</attribute>
            </button>
        </field>
    </record>
</odoo>
