<odoo>
<data>

    <record id="inherit_survey_form_auto_follow_up_view" model="ir.ui.view">
        <field name="name">Survey form Inherit</field>
        <field name="model">survey.survey</field>
        <field name="inherit_id" ref="survey.survey_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form" position="attributes">
                    <attribute name="class">menu_hr_recruitment_sticky</attribute>
            </xpath>
            <xpath expr="//field[@name='access_mode']" position="after">
                <field name="is_manual_test" string="Manual Test"  />
            </xpath>
           
            <xpath expr="//group[@name='candidates']/div" position="after">
                    <field name="is_personality_and_emotional" invisible="1"/>
                    <field name="is_setting_psychological_test" invisible="1"/>
                    <field name="is_auto_next_stage" attrs="{'invisible':['|', ('is_personality_and_emotional','!=',True), ('is_setting_psychological_test','=',True)]}" />
                    <field name="is_follow_up" string="Auto Follow Up"/>
                    <field name="email_template_id" attrs="{'invisible':[('is_follow_up','!=',True)]}" />
                    <field name="wa_template_id" attrs="{'invisible':[('is_follow_up','!=',True)]}"  string="Whatsapp Template"/>
                    <label for="interval_number" attrs="{'invisible':[('is_follow_up','!=',True)]}"  string="Execute Every"/>
                    <div attrs="{'invisible':[('is_follow_up','!=',True)]}"  >
                        <field name="interval_number" class="oe_inline" attrs="{'invisible':[('is_follow_up','!=',True)],'required':[('is_follow_up','=',True)]}" />
                        <field name="interval_type" class="oe_inline" attrs="{'invisible':[('is_follow_up','!=',True)],'required':[('is_follow_up','=',True)]}"/>
                    </div>
                    <field name="number_of_repetion" string="Number of Repetitions" attrs="{'invisible':[('is_follow_up','!=',True)],'required':[('is_follow_up','=',True)]}" />
            </xpath>
       
            
        </field>
    </record>

    <record id="inherit_survey_tree_auto_follow_up_view" model="ir.ui.view">
        <field name="name">Survey Tree Inherit</field>
        <field name="model">survey.survey</field>
        <field name="inherit_id" ref="survey.survey_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//tree" position="attributes">
                <attribute name="class">menu_hr_recruitment_sticky</attribute>
            </xpath>
        </field>
    </record>


</data>


</odoo>