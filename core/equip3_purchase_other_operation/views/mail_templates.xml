<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="email_template_purchase_tender_invitation_portal" model="mail.template">
            <field name="name">Reminder for Purchase Tender Invitation Portal</field>
            <field name="email_from">${ctx.get('email_from')}</field>
            <field name="email_to">${ctx.get('email_to')}</field>
            <field name="subject">Reminder for Purchase Tender Invitation Portal</field>
            <field name="model_id" ref="equip3_purchase_other_operation.model_purchase_agreement"/>
            <field name="auto_delete" eval="True"/>
            <field name="body_html"><![CDATA[
                    <p>Date: ${ctx.get('date')}</p>
                    <p>Subject : Reminder for Purchase Tender Invitation Portal</p>
                    </br>
                    <p>Dear ${ctx.get('partner_name')},</p>
                    <p>Based on the Purchase Tender Invitation Request ${object.name}. You have been invited to submit a Quotation.</p></br>
                    </br>
                    <p>Thank you.</p>
                    ]]>
            </field>
        </record>
    </data>
</odoo>