<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="email_template_purchase_currency_approval_matrix" model="mail.template">
        <field name="name">Internal Approval Matrix</field>
        <field name="email_from">${ctx.get('email_from')}</field>
        <field name="email_to">${ctx.get('email_to')}</field>
        <field name="subject">Request For Approval</field>
        <field name="model_id" ref="equip3_accounting_multicurrency.model_account_internal_transfer"/>
        <field name="auto_delete" eval="True"/>
        <field name="body_html"><![CDATA[
            <p>Date: ${ctx.get('date')}</p>
            <p>Subject: Application for Purchase Currency Approval</p>
            <p>Dear ${ctx.get('approver_name')},</p>
            <p>You have approval pending that needs your decision to approve or reject the purchase currency.</p>
            <p>${object.request_partner_id.name} has requested a ${ctx.get('currency')} purchase currency on ${ctx.get('date_invoice')}.
            </p>
            <p>You can approve or reject the purchase currency by clicking this button: </p>
            <div style = "margin: 16px;">
                <a href=${ctx.get('url')} style = "padding: 5px 10px; font-size: 12px; line-height: 18px;
                 color: #FFFFFF; border-color:#875A7B; text-decoration: none; display: inline-block; 
                 margin-bottom: 0px; font-weight: 400; text-align: center; vertical-align: middle; 
                 cursor: pointer; white-space: nowrap; background-image: none; background-color: #875A7B;
                 border: 1px solid #875A7B; border-radius:3px">View Purchase Currency</a></div>
            <p>Thank you,</p>
            <p>This is an automatically generated email, please do not reply.</p>
            ]]></field>
    </record>

    <record id="email_template_purchase_currency_approval_submitter_matrix" model="mail.template">
        <field name="name">Submitter Approval Matrix</field>
        <field name="email_from">${ctx.get('email_from')}</field>
        <field name="email_to">${ctx.get('email_to')}</field>
        <field name="subject">Submitter Approval</field>
        <field name="model_id" ref="equip3_accounting_multicurrency.model_account_internal_transfer"/>
        <field name="auto_delete" eval="True"/>
        <field name="body_html"><![CDATA[
            <p>Date: ${ctx.get('date')}</p>
            <p>Subject: Approval of Purchase Currency</p>
            <p>Dear ${object.request_partner_id.name},</p>
            <p>We have received a response to your request for a Purchase Currency on ${ctx.get('create_date')}.
            </p>
            <p>This notification serves as confirmation that your purchase currency has been <b>approved</b>.
            </p>
            <p>Thank you,</p>
            <p>This is an automatically generated email, please do not reply.</p>
            ]]></field>
    </record>

    <record id="email_template_purchase_currency_approval_rejected_matrix" model="mail.template">
        <field name="name">Rejected Approval Matrix</field>
        <field name="email_from">${ctx.get('email_from')}</field>
        <field name="email_to">${ctx.get('email_to')}</field>
        <field name="subject">Rejected Approval</field>
        <field name="model_id" ref="equip3_accounting_multicurrency.model_account_internal_transfer"/>
        <field name="auto_delete" eval="True"/>
        <field name="body_html"><![CDATA[
            <p>Date: ${ctx.get('date')},</p>
            <p>Subject: Rejection of Purchase Currency</p>
            <p>Dear ${object.request_partner_id.name},</p>
            <p>We have received a response to your request for a Purchase Currency on ${ctx.get('create_date')}.
            </p>
            <p>This notification serves as confirmation that your purchase currency has been <b>rejected</b>.
            </p>
            <p>Here is the feedback from ${ctx.get('rejected_name')}</p>
            <p>${ctx.get('feedback')}</p>
            <p>Thank you,</p>
            <p>This is an automatically generated email, please do not reply.</p>
            ]]></field>
    </record>

    <record id="wa_template_purchase_currency_approval_matrix" model="mail.template">
        <field name="name">Internal Approval Matrix</field>
        <field name="email_from">${ctx.get('email_from')}</field>
        <field name="email_to">${ctx.get('email_to')}</field>
        <field name="subject">Request For Approval</field>
        <field name="model_id" ref="equip3_accounting_multicurrency.model_account_internal_transfer"/>
        <field name="auto_delete" eval="True"/>
        <field name="body_html">
            <![CDATA[<div style="font-family: 'Lucica Grande', Ubuntu, Arial, Verdana, sans-serif; font-size: 12px;">
            <p style="margin: 0px; padding: 0px; font-size: 13px;">
                Dear ${approver_name},${br}
                You have approval pending that needs your decision to approve or reject the purchase currency.${br}
                ${submitter_name} has requested a ${currency} purchase currency on ${transfer_date}${br}
                You can approve or reject the purchase currency by clicking this link: 
                ${url}${br}
                Thank you,
                This is an automatically generated message, please do not reply.
            </p>
            ]]>
        </field>
    </record>

    <record id="wa_template_purchase_currency_approval_submitter_matrix" model="mail.template">
        <field name="name">Submitter Approval Matrix</field>
        <field name="email_from">${ctx.get('email_from')}</field>
        <field name="email_to">${ctx.get('email_to')}</field>
        <field name="subject">Submitter Approval</field>
        <field name="model_id" ref="equip3_accounting_multicurrency.model_account_internal_transfer"/>
        <field name="auto_delete" eval="True"/>
        <field name="body_html">
            <![CDATA[<div style="font-family: 'Lucica Grande', Ubuntu, Arial, Verdana, sans-serif; font-size: 12px;">
            <p style="margin: 0px; padding: 0px; font-size: 13px;">
                Dear ${submitter_name},${br}
                We have received a response to your request for a Purchase Currency on ${create_date}.${br}
                This notification serves as confirmation that your purchase currency has been approved.${br}
                Thank you,
                This is an automatically generated message, please do not reply.
            </p>
            ]]>
        </field>
    </record>

    <record id="wa_template_purchase_currency_approval_rejected_matrix" model="mail.template">
        <field name="name">Rejected Approval Matrix</field>
        <field name="email_from">${ctx.get('email_from')}</field>
        <field name="email_to">${ctx.get('email_to')}</field>
        <field name="subject">Rejected Approval</field>
        <field name="model_id" ref="equip3_accounting_multicurrency.model_account_internal_transfer"/>
        <field name="auto_delete" eval="True"/>
        <field name="body_html">
            <![CDATA[<div style="font-family: 'Lucica Grande', Ubuntu, Arial, Verdana, sans-serif; font-size: 12px;">
            <p style="margin: 0px; padding: 0px; font-size: 13px;">
                Dear ${submitter_name},${br}
                We have received a response to your request for a Purchase Currency on ${create_date}.${br}
                This notification serves as confirmation that your purchase currency has been rejected.${br}
                Here is the feedback from ${approver_name}:
                ${feedback}${br}
                Thank you,
                This is an automatically generated message, please do not reply.
            </p>
            ]]>
        </field>
    </record>

</odoo>
