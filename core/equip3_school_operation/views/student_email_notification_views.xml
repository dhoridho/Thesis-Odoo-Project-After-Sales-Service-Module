<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
    <!--Assignment Notification-->
        <record id="student_assignment_notification" model="mail.template">
            <field name="name">Assignment Notification</field>
            <field name="email_from">${object.email_from | safe}</field>
            <field name="email_to">${object.email | safe}</field>
            <field name="subject">ASSIGNMENT NOTIFICATION</field>
            <field name="model_id" ref="assignment.model_school_teacher_assignment"/>
            <field name="auto_delete" eval="False"/>
            <field name="body_html"><![CDATA[
            <div>
                <p>Dear ${object.student_name},</p>
                <p>${object.teacher_id.name} posted a new assignment ${ctx['url']}.</p>

                <p>Below are the confirmed details for your assignment:</p>
                <p>Name of Student 	: ${object.student_name}</p>
                <p>Class			: ${object.standard_id.name}</p>
                <p>Academic Year 	: ${object.year.name}</p>
                <p>Teacher Name     : ${object.teacher_id.name}</p>
                <p>Assignment Name	: ${object.name}</p>
                <p>Subject		    : ${object.subject_id.name}</p>
                <p>Assign Date 		: ${object.assign_date}</p>
                <p>Due Date		    : ${object.due_date}</p>

                <p>Please check the details in the attachment.</p>
                <p>Thank you for read this message notification.</p>
                <p>Please do not reply to this notification as it as automated and is unable to receive replies.</p>

            </div>
            ]]></field>
        </record>

        <!--Exam Notification-->
        <record id="student_exam_notification" model="mail.template">
            <field name="name">Exam Notification</field>
            <field name="email_from">${object.user_ids.email | safe}</field>
            <field name="email_to">${object.student_name.name | safe}</field>
            <field name="subject">EXAM NOTIFICATION</field>
            <field name="model_id" ref="exam.model_exam_exam"/>
            <field name="auto_delete" eval="False"/>
            <field name="body_html"><![CDATA[
            <div>
                <p>Dear ${object.student_name},</p>
                <p>${object.sender_name} posted a new exam ${ctx['url']}.</p>

                <p>Below are the confirmed details for your exam:</p>
                <p>Name of Student 	: ${object.student_name}</p>
                <p>Class			: ${object.standard_id.name}</p>
                <p>Academic Year 	: ${object.academic_year.name}</p>
                <p>Exam Code		: ${object.exam_code}</p>
                <p>Exam Name		: ${object.name}</p>
                <p>Exam Start Date 	: ${object.start_date}</p>
                <p>Exam End Date	: ${object.end_date}</p>

                Thank you for read this message notification.
                Please do not reply to this notification as it as automated and is unable to receive replies.

            </div>
            ]]></field>
        </record>
    </data>
</odoo>
