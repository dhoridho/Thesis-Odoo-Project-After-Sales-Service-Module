<?xml version='1.0' encoding='utf-8'?>
<odoo>
    <data noupdate="0">
        <record id="recurring_invoice_contract_property_scheduler" model="ir.cron">
            <field name="name">Recurring Invoice Contract Property</field>
            <field name="model_id" ref="model_agreement"/>
            <field name="state">code</field>
            <field name="code">model.create_invoice_property_agreement()</field>
            <field name="user_id" ref="base.user_root" />
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="nextcall" eval="datetime.now() + (datetime.now(pytz.timezone('Asia/Jakarta')).replace(hour=00, minute=1, second=0) - datetime.now(pytz.timezone('Asia/Jakarta'))) % timedelta(hours=24)"/>
            <field name="numbercall">-1</field>
            <field eval="True" name="doall" />
        </record>
    </data>
</odoo>