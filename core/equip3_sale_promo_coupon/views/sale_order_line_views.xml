<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_sale_order_form_inherit" model="ir.ui.view">
        <field name="name">Sale Order Form</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='payment_term_id']" position="after">
                <field name="is_valid_coupon" invisible="1"/>
            </xpath>
            <xpath expr="//page/field[@name='order_line']" position="attributes">
                <attribute name="attrs">{'readonly': ['|',('state', 'in', ['done', 'cancel']),('is_valid_coupon','=',True)]}</attribute>
            </xpath>
        </field>
    </record>

    <record id="sale_order_view_form_inherit" model="ir.ui.view">
        <field name="name">sale.order.form.inherit.coupon</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale_coupon.sale_order_view_form"/>
        <field name="arch" type="xml">
           <xpath expr="//div/button[@name='recompute_coupon_lines']" position="after">
                <button name="action_cancel_promo" class="btn btn-secondary" string="Cancel Promo"
                        type="object" states="draft,sent,sale"/>
           </xpath>
        </field>
   </record>

</odoo>
