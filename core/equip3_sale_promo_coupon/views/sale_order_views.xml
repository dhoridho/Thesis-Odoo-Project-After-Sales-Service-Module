<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="sale_order_view_form" model="ir.ui.view">
        <field name="name">sale.order.view.form</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale_coupon.sale_order_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//button[@name='recompute_coupon_lines']" position="after">
                <button name="recompute_coupon_lines_equip" class="btn btn-secondary" string="Promotions"
                        help="When clicked, the content of the order will be checked to detect (and apply) possible promotion programs."
                        type="object" states="draft,sent,sale"/>
                <button name="action_cancel_promo" type="object" class="btn btn-secondary" states="draft,sent,sale" string="Cancel Promo"/>
            </xpath>
            <xpath expr="//button[@name='recompute_coupon_lines']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
        </field>
    </record>

    <record id="sale_order_form_view_inherit_readonly_lock" model="ir.ui.view">
        <field name="name">sale.order.view.form</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="equip3_sale_operation.sale_order_form_view_inherit_readonly_lock"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='partner_id']" position="after">
                <field name="lock_order_line" invisible="1"/>
            </xpath>
            <xpath expr="//field[@name='order_line']" position="attributes">
                <attribute name="attrs">{'readonly': ['|', ('lock_order_line', '=', True), '&amp;',('state', 'in', ['sale','quotation_approved']), ('lock_sale_order','=',True)]}</attribute>
            </xpath>
        </field>
    </record>
</odoo>