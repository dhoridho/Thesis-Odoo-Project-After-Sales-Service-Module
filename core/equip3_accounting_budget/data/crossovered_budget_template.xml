<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="email_template_budgets_matrix_approve_request" model="mail.template">
        <field name="name">Application for Budget Approval</field>
        <field name="email_from">${ctx.get('email_from')}</field>
        <field name="email_to">${ctx.get('email_to')}</field>
        <field name="subject">Request For Approval</field>
        <field name="model_id" ref="equip3_accounting_budget.model_crossovered_budget"/>
        <field name="auto_delete" eval="True"/>
        <field name="body_html"><![CDATA[
            <p>Date: ${ctx.get('date')}</p>
            <p>Subject: Application for Budget Approval</p>
            <p>Dear ${ctx.get('approver_name')},</p>
            <p>You have approval pending that needs your decision to approve or reject the budget.</p>
            <p>${object.request_partner_id.name} has requested a budget amount of ${ctx.get('currency')} for ${object.branch_id.name} from ${object.date_from} to ${object.date_to}
            </p>
            <p>You can approve or reject the budget by clicking this button: </p>
            <div style = "margin: 16px;">
                <a href=${ctx.get('url')} style = "padding: 5px 10px; font-size: 12px; line-height: 18px;
                 color: #FFFFFF; border-color:#875A7B; text-decoration: none; display: inline-block; 
                 margin-bottom: 0px; font-weight: 400; text-align: center; vertical-align: middle; 
                 cursor: pointer; white-space: nowrap; background-image: none; background-color: #875A7B;
                 border: 1px solid #875A7B; border-radius:3px">View Budget</a></div>
            <p>Thank you,</p>
            <p>This is an automatically generated email, please do not reply.</p>
            ]]></field>
    </record>

    <record id="email_template_approval_of_budget_action_approve" model="mail.template">
        <field name="name">Approval of Budget</field>
        <field name="email_from">${ctx.get('email_from')}</field>
        <field name="email_to">${ctx.get('email_to')}</field>
        <field name="subject">Submitter Approval</field>
        <field name="model_id" ref="equip3_accounting_budget.model_crossovered_budget"/>
        <field name="auto_delete" eval="True"/>
        <field name="body_html"><![CDATA[
            <p>Date: ${ctx.get('date')}</p>
            <p>Subject: Approval of Budget</p>
            <p>Dear ${object.request_partner_id.name},</p>
            <p>We have received a response to your request for a Budget on ${ctx.get('create_date')}.
            </p>
            <p>This notification serves as confirmation that your budget has been <b>approved</b>.
            </p>
            <p>Thank you,</p>
            <p>This is an automatically generated email, please do not reply.</p>
            ]]></field>
    </record>

    <record id="email_template_rejection_of_budget" model="mail.template">
        <field name="name">Rejection of Budget</field>
        <field name="email_from">${ctx.get('email_from')}</field>
        <field name="email_to">${ctx.get('email_to')}</field>
        <field name="subject">Rejected Approval</field>
        <field name="model_id" ref="equip3_accounting_budget.model_crossovered_budget"/>
        <field name="auto_delete" eval="True"/>
        <field name="body_html"><![CDATA[
            <p>Date: ${ctx.get('date')},</p>
            <p>Subject: Rejection of Budget</p>
            <p>Dear ${object.request_partner_id.name},</p>
            <p>We have received a response to your request for a Budget on ${ctx.get('create_date')}.
            </p>
            <p>This notification serves as confirmation that your budget has been <b>rejected</b>.
            </p>
            <p>Here is the feedback from ${ctx.get('rejected_name')}</p>
            <p>${ctx.get('feedback')}</p>
            <p>Thank you,</p>
            <p>This is an automatically generated email, please do not reply.</p>
            ]]></field>
    </record>

</odoo>
