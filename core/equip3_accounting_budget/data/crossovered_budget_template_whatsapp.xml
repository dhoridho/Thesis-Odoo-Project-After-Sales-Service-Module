<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="email_template_budgets_matrix_approve_request_whatsapp" model="mail.template">
        <field name="name">Application for Budget Approval</field>
        <field name="email_from">${ctx.get('email_from')}</field>
        <field name="email_to">${ctx.get('email_to')}</field>
        <field name="subject">Request For Approval</field>
        <field name="model_id" ref="equip3_accounting_budget.model_crossovered_budget"/>
        <field name="auto_delete" eval="True"/>
        <field name="body_html"><![CDATA[
            Dear ${approver_name},${br}
            You have approval pending that needs your decision to approve or reject the budget.${br}
            ${submitter_name} has requested a ${currency} budget with period ${date_from} to ${date_to}.${br}
            You can approve or reject the budget by clicking this link: ${br}
            ${url}${br} 
            Thank you,${br}
            This is an automatically generated whatsapp, please do not reply.
            ]]></field>
    </record>

    <record id="email_template_approval_of_budget_action_approve_whatsapp" model="mail.template">
        <field name="name">Approval of Budget</field>
        <field name="email_from">${ctx.get('email_from')}</field>
        <field name="email_to">${ctx.get('email_to')}</field>
        <field name="subject">Submitter Approval</field>
        <field name="model_id" ref="equip3_accounting_budget.model_crossovered_budget"/>
        <field name="auto_delete" eval="True"/>
        <field name="body_html"><![CDATA[
            Dear ${recipient_name},${br}
            We have received a response to your request for an Budget on ${create_date}.${br}
            This notification serves as confirmation that your budget has been approved.${br}         
            Thank you,${br}
            This is an automatically generated whatsapp, please do not reply.
            ]]></field>
    </record>

    <record id="email_template_rejection_of_budget_whatsapp" model="mail.template">
        <field name="name">Rejection of Budget</field>
        <field name="email_from">${ctx.get('email_from')}</field>
        <field name="email_to">${ctx.get('email_to')}</field>
        <field name="subject">Rejected Approval</field>
        <field name="model_id" ref="equip3_accounting_budget.model_crossovered_budget"/>
        <field name="auto_delete" eval="True"/>
        <field name="body_html"><![CDATA[
            Dear ${recipient_name},${br}
            We have received a response to your request for an Budget on ${create_date}.${br}
            This notification serves as confirmation that your budget has been rejected.${br} 
            Here is the feedback from ${rejecter_user}${br}
            ${feedback}${br}
            Thank you,${br}
            This is an automatically generated whatsapp, please do not reply.
            ]]></field>
    </record>

</odoo>
