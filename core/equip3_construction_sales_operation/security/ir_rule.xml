<?xml version="1.0" encoding="utf-8"?>

<odoo>
    <data>

        <record id="job_estimate_multi_branch_rule" model="ir.rule">
            <field name="name">Job Estimate Multi-Branch</field>
            <field name="model_id" ref="model_job_estimate"/>
            <field name="domain_force">[
                '|', 
                    ('branch_id', '=', False), 
                    ('branch_id', 'in', branch_ids)
            ]</field>
        </record>

        <record id="sales_multi_branch_rule" model="ir.rule">
            <field name="name">Sales Order Multi-Branch</field>
            <field name="model_id" ref="model_sale_order_const"/>
            <field name="domain_force">[
                '|', 
                    ('branch_id', '=', False), 
                    ('branch_id', 'in', branch_ids)
            ]</field>
        </record>

        <record id="job_estimate_approval_multi_branch_rule" model="ir.rule">
            <field name="name">Job Estimate Approval Multi-Branch</field>
            <field name="model_id" ref="model_approval_matrix_job_estimates"/>
            <field name="domain_force">[
                '|', 
                    ('branch_id', '=', False), 
                    ('branch_id', 'in', branch_ids)
            ]</field>
        </record>
        
        <record id="sales_approval_multi_branch_rule" model="ir.rule">
            <field name="name">Sales Order Approval Multi-Branch</field>
            <field name="model_id" ref="model_approval_matrix_sale_order_const"/>
            <field name="domain_force">[
                '|', 
                    ('branch_id', '=', False), 
                    ('branch_id', 'in', branch_ids)
            ]</field>
        </record>

    </data>
</odoo>