<?xml version="1.0"?>
<odoo>

    <record id="view_stock_inventory_line_add_new_field" model="ir.ui.view" >
        <field name="name">Stock Inventory Add New Field for Inventory OPS APP</field>
        <field name="model">stock.inventory.line</field>
        <field name="inherit_id" ref="equip3_inventory_control.stock_inventory_line_tree_inherit"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='uom_id']" position="after">
                <field name="c" invisible="1"/>
            </xpath>
        </field>
    </record>

    <!--<record id="stock_inventory_form_add_tab" model="ir.ui.view">
        <field name="name">Stock Inventory Added New Tab For App</field>
        <field name="model">stock.inventory</field>
        <field name="inherit_id" ref="sh_all_in_one_mbs.sh_inven_adjt_barcode_mobile_inventory_form"/>
        <field name="arch" type="xml">
            <xpath expr="//page[1]" position="before">
                <page string="Products" attrs="{'invisible':[('state','=','draft')]}">
                    <field name="app_line_ids" readonly="1">
                        <tree editable="bottom" colors="red: state=='cancel'">
                            <field name="product_id"/>
                            <field name="location_id"/>
                            <field name="package_id"/>
                            <field name="state" invisible="1"/>
                            <field name="existing_qty"/>
                            <button name="view_existing_data" string="View Existing Quants" type="object"
                                    icon="fa-list"/>
                            <field name="count_qty"/>
                            <field name="tracking" invisible="1"/>
                            <button name="view_count_data" string="View Stock Count" type="object" icon="fa-list"
                                    attrs="{'invisible': [('tracking', '=', 'none')]}"/>
                        </tree>
                    </field>
                </page>
            </xpath>

            <xpath expr="//page[2]" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>

            <xpath expr="//button[@name='action_start']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>

            <button name="action_start" position="after">
                <button name="action_app_confirm" states="draft"
                        string="Start Inventory" type="object" class="oe_highlight"
                        groups="stock.group_stock_user"/>
            </button>
        </field>
    </record>

    <record id="stock_inventory_form_hide_button" model="ir.ui.view">
        <field name="name">Stock Inventory Form hide button</field>
        <field name="model">stock.inventory</field>
        <field name="inherit_id" ref="equip3_inventory_control.inherit_stock_inventory_form_view"/>
        <field name="arch" type="xml">
            <xpath expr="//button[@name='action_complete']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>

            <button name="action_complete" position="after">
                <button name="action_app_complete" states="confirm"
                        string="Complete Inventory" type="object" class="oe_highlight"
                        groups="stock.group_stock_user"/>
            </button>

            <button name="set_to_draft_inv" position="attributes">
                <attribute name="invisible">1</attribute>
            </button>

            <button name="action_cancel_draft"  position="attributes">
                <attribute name="attrs">{'invisible': [('state', 'not in' , ['confirm','completed','to_approve'])]}</attribute>
            </button>
        </field>
    </record>-->
</odoo>