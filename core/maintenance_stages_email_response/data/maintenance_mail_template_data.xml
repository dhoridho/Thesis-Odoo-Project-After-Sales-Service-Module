<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

    <record id="custom_mail_template_data_maintenance_new" model="mail.template">
            <field name="name">Stages: Reception of New Request</field>
            <field name="model_id" ref="maintenance.model_maintenance_request"/>
            <field name="subject">Maintenance Request of ${object.name}</field>
            <field name="use_default_to" eval="True"/>
            <field name="body_html" type="html">
                <div>
                    Dear ${object.user_id.name or 'User'},<br/><br/>
                    We have received your maintenance request and our maintenance team is working it with: <b>${object.stage_id.name}</b> Stage.<br/><br />We will keep you posted! <br /><br /><br/>
                    Thank you.<br /><br/>
                    If you have any questions, please let us know.
                    <br/><br/>
                    Thank you,<br/>
                    % if object.user_id:
                        <b>${object.user_id.name}</b>
                        <br/>Email: ${object.user_id.email or ''}
                        <br/>Phone: ${object.user_id.phone or ''}
                    % else:
                        ${object.company_id.name}
                    % endif
                </div>
            </field>
                <field name="lang">${object.user_id.lang}</field>
                <field name="auto_delete" eval="True"/>
        </record>

        <record id="custom_mail_template_data_in_progress" model="mail.template">
            <field name="name">Stages : Reception of In Progress</field>
            <field name="model_id" ref="maintenance.model_maintenance_request"/>
            <field name="subject">Maintenance Request of ${object.name}</field>
            <field name="use_default_to" eval="True"/>
            <field name="body_html" type="html">
                <div>
                    Dear ${object.user_id.name or 'User'},<br/><br/> We are working on maintenance request with: <b>${object.stage_id.name}</b> Stage.<br/><br />We will keep you posted! <br /><br /><br/> Thank you.<br /><br/>
                    % if object.user_id:
                        <b>${object.user_id.name}</b>
                        <br/>Email: ${object.user_id.email or ''}
                        <br/>Phone: ${object.user_id.phone or ''}
                    % else:
                        ${object.company_id.name}
                    % endif
                </div>
            </field>
                <field name="lang">${object.user_id.lang}</field>
                <field name="auto_delete" eval="True"/>
        </record>

        <record id="custom_mail_template_proposition_required" model="mail.template">
            <field name="name">Stages: Reception of Repaired</field>
            <field name="model_id" ref="maintenance.model_maintenance_request"/>
            <field name="subject">Maintenance Request of ${object.name}</field>
            <field name="use_default_to" eval="True"/>
            <field name="body_html" type="html">
                <div> 
                    Dear ${object.user_id.name or 'User'},<br/><br/> We are working on maintenance request with: <b>${object.stage_id.name}</b> Stage.<br/><br />We will keep you posted! <br /><br /><br/> Thank you.<br /><br/>
                    % if object.user_id:
                        <b>${object.user_id.name}</b>
                        <br/>Email: ${object.user_id.email or ''}
                        <br/>Phone: ${object.user_id.phone or ''}
                    % else:
                        ${object.company_id.name}
                    % endif
                </div>
            </field>
                <field name="lang">${object.user_id.lang}</field>
                <field name="auto_delete" eval="True"/>
        </record>
    </data>
</odoo>


