<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="ceisa_user_incompany_form_view" model="ir.ui.view">
        <field name="name">ceisa.user.incompany.form</field>
        <field name="model">res.company</field>
        <field name="inherit_id" ref="base.view_company_form"/>
        <field name="mode">primary</field>
        <field name="arch" type="xml">
            <xpath expr="//notebook" position="inside">
                <page string="Ceisa User" name="ceisa_user" attrs="{'invisible':[('is_ceisa_it_inventory', '=', False)]}" >
<!--                <page string="Ceisa User" name="ceisa_user">-->
                    <group>
                        <group name="ceisa_user_information">
                            <field name="is_ceisa_it_inventory" invisible="1"/>
                            <field name="ceisa_user" required="1"/>
                            <field name="ceisa_password" password="True" required="1"/>
                            <button name="submit_ceisa_user" string="Submit" type="object"/>
                        </group>
                    </group>
                </page>
            </xpath>
        </field>
    </record>
</odoo>