<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="email_template_asset_approval_matrix" model="mail.template">
        <field name="name">Assets Approval Matrix</field>
        <field name="email_from">${ctx.get('email_from')}</field>
        <field name="email_to">${ctx.get('email_to')}</field>
        <field name="subject">Request For Approval</field>
        <field name="model_id" ref="om_account_asset.model_account_asset_asset"/>
        <field name="auto_delete" eval="True"/>
        <field name="body_html"><![CDATA[
            <p>Date ${ctx.get('date')},</p>
            <p>Subject : Application for Assets Approval</p>
            <p>Dear ${ctx.get('approver_name')},</p>
            <p>You have approval pending that needs your decision to approve or reject the assets.</p>
            <p>${object.request_partner_id.name} has requested a ${ctx.get('name_asset')} as ${ctx.get('category_id_asset')} for ${ctx.get('value_asset')} ${ctx.get('currency_id_asset')}.
            </p>
            <p>You can approve or reject the assets by clicking this button:</p>
            <div style = "margin: 16px;">
                <a href=${ctx.get('url')} style = "padding: 5px 10px; font-size: 12px; line-height: 18px;
                 color: #FFFFFF; border-color:#875A7B; text-decoration: none; display: inline-block; 
                 margin-bottom: 0px; font-weight: 400; text-align: center; vertical-align: middle; 
                 cursor: pointer; white-space: nowrap; background-image: none; background-color: #875A7B;
                 border: 1px solid #875A7B; border-radius:3px">View Assets</a></div>
            <p>Thank you,</p>
            <p>This is an automatically generated email, please do not reply.</p>
            ]]></field>
    </record>

    <record id="email_template_asset_submitter_approval_matrix" model="mail.template">
        <field name="name">Submitter Approval Matrix</field>
        <field name="email_from">${ctx.get('email_from')}</field>
        <field name="email_to">${ctx.get('email_to')}</field>
        <field name="subject">Submitter Approval</field>
        <field name="model_id" ref="om_account_asset.model_account_asset_asset"/>
        <field name="auto_delete" eval="True"/>
        <field name="body_html"><![CDATA[
            <p>Date ${ctx.get('date')},</p>
            <p>Subject : Approval of Assets</p>
            <p>Dear ${object.request_partner_id.name}:</p>
            <p>We have received a response to your request for an Assets on ${ctx.get('create_date')}.
            </p>
            <p>This notification serves as confirmation that your assets has been approved.
            </p>
            <p>Thank you,</p>
            <p>This is an automatically generated email, please do not reply.</p>
            ]]></field>
    </record>

    <record id="email_template_asset_rejected_matrix" model="mail.template">
        <field name="name">Rejected Approval Matrix</field>
        <field name="email_from">${ctx.get('email_from')}</field>
        <field name="email_to">${ctx.get('email_to')}</field>
        <field name="subject">Rejected Approval</field>
        <field name="model_id" ref="om_account_asset.model_account_asset_asset"/>
        <field name="auto_delete" eval="True"/>
        <field name="body_html"><![CDATA[
            <p>Date ${ctx.get('date')},</p>
            <p>Subject : Rejection of Assets</p>
            <p>Dear ${object.request_partner_id.name}:</p>
            <p>We have received a response to your request for an Assets on ${ctx.get('create_date')}.
            </p>
            <p>This notification serves as confirmation that your assets has been rejected.
            </p>
            <p>Here is the feedback from ${ctx.get('rejected_name')}</p>
            <p>${ctx.get('feedback')}</p>
            <p>Thank you,</p>
            <p>This is an automatically generated email, please do not reply.</p>
            ]]></field>
    </record>

</odoo>
