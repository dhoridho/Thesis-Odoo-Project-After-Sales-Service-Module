<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="receipt_voucher_view_tree" model="ir.ui.view">
            <field name="name">receipt.voucher.view.tree</field>
            <field name="model">receipt.voucher</field>
            <field name="arch" type="xml">
                <tree string="Receipt Voucher">
                    <field name="partner_ids" widget="many2many_tags"/>
                    <field name="name"/>
                    <field name="voucher_date"/>
                    <field name="amount" style="text-align: center;"/>
                    <field name="state" optional="show" widget="badge" decoration-success="state == 'paid'" decoration-primary="state =='verified'" decoration-success2="state == 'submitted'" decoration-muted="state == 'draft'" decoration-danger3="state == 'canceled'" decoration-danger="state == 'rejected'" decoration-warning3="state == 'to_approve'" decoration-warning2="state == 'approved'"/>
                </tree>
            </field>
        </record>
        
        <record id="receipt_voucher_view_form" model="ir.ui.view">
            <field name="name">receipt.voucher.view.form</field>
            <field name="model">receipt.voucher</field>
            <field name="arch" type="xml">
                <form string="Receipt Voucher">
                    <header>
                        <button name="action_submit" string="Submit" class="oe_highlight" type="object" states="draft" />
                        <button name="action_verify" string="Verify" class="oe_highlight" type="object" states="submitted" />
                        <button name="action_cancel" string="Cancel" class="oe_highlight" type="object" states="draft,submitted" />
                        <button name="action_pay" string="Pay" class="oe_highlight" type="object" states="verified" />
                        <button name="set_to_draft" string="Set to Draft" class="oe_highlight" type="object" states="canceled" />
                        <field name="state" widget="statusbar" statusbar_visible="draft,submitted,verified,paid"/>
                    </header>
                    <sheet>
                        <div name="button_box" class="oe_button_box">
                            <button class="oe_stat_button" type="object" name="action_view_payment" icon="o-hm-smart-all-3-payments">
                                <field string="Payments" name="payment_count" widget="statinfo"/>
                            </button>
                        </div>
                        <div class="oe_title">
                            <h1>
                                <field name="name" readonly="1"/>
                            </h1>
                        </div>
                        <group>
                            <group>
                                <field name="is_cutoff_date" invisible ="1"/>
                                <field name="partner_id" string="Customer" widget="many2one_tags" invisible="1"/>
                                <field name="partner_ids" string="Customer" widget="many2many_tags" attrs="{'readonly':[('state','!=','draft')]}" required="1" domain="[('is_customer','=',True)]"/>
                                <field name="voucher_date"/>
                                <field name="invoice_cutoff_date" readonly="1" force_save="1" attrs="{'invisible': [('is_cutoff_date', '=', False)]}"/>
                                <field name="bank_id"/>
                                <field name="cheque_number" attrs="{'readonly':[('state','!=','draft')]}"/>
                                <field name="cheque_date" attrs="{'readonly':[('state','!=','draft')]}"/>
                                <field name="create_by" readonly="1"/>
                                <field name="create_date" readonly="1"/>
                            </group>
                            <group>
                                <field name="customer_allowed_ids" invisible="1"/>
                                <field name="customer_invoice_ids" widget="many2many_tags"  attrs="{'readonly':[('state','!=','draft')]}" required="1"/>
                                <field name="currency_id" attrs="{'readonly':[('state','!=','draft')]}"/>
                                <field name="remarks" string="Remarks" attrs="{'readonly':[('state','!=','draft')]}"/>
                                <field name="company_id" readonly="1"/>
                                <field name="branch_id" attrs="{'readonly':[('state','!=','draft')]}" required="1" options="{'no_create': True, 'no_create_edit': True}"/>
                            </group>
                        </group>
                        <notebook>
                            <page name="receipt_lines" string="Invoice Info">
                                <group name="invoice_info" string="Invoice Information">
                                    <field name="receipt_voucher_line_ids" nolabel="1">
                                        <tree editable="top" create="false" delete="false">
                                            <field name="invoice_id" readonly="1"/>
                                            <field name="partner_id" readonly="1"/>
                                            <field name="invoice_date" readonly="1"/>
                                            <field name="partner_id" readonly="1"/>
                                            <field name="untaxed_amount" widget='monetary' options="{'currency_field': 'currency_id'}" readonly="1"/>
                                            <field name="tax_amount" widget='monetary' options="{'currency_field': 'currency_id'}" readonly="1"/>
                                            <field name="total_invoice_amount" widget='monetary' options="{'currency_field': 'currency_id'}" readonly="1"/>
                                            <field name="amount_due" widget='monetary' options="{'currency_field': 'currency_id'}" readonly="1"/>
                                            <field name="amount" widget='monetary' options="{'currency_field': 'currency_id'}" required="1"/>
                                            
                                        </tree>
                                    </field>
                                </group>
                            </page>
                        </notebook>
                    </sheet>
                </form>
            </field>
        </record>


        <record id="receipt_voucher_action" model="ir.actions.act_window">
            <field name="name">Receipt Voucher</field>
            <field name="res_model">receipt.voucher</field>
            <field name="view_mode">tree,form</field>
            <field name="view_ids" eval="[(5, 0, 0),
                (0, 0, {'view_mode': 'tree', 'view_id': ref('equip3_accounting_operation.receipt_voucher_view_tree')}),
                (0, 0, {'view_mode': 'form', 'view_id': ref('equip3_accounting_operation.receipt_voucher_view_form')})]"/>
        </record>

        <menuitem action="receipt_voucher_action" id="menu_action_account_receipt_voucher_action" groups="account.group_account_readonly" parent="equip3_accounting_operation.menu_customer_receipt" sequence="2"/>
    </data>
</odoo>