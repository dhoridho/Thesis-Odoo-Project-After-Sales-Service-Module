<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="email_template_receipt_approval_matrix" model="mail.template">
        <field name="name">Request Approval Matrix</field>
        <field name="email_from">${ctx.get('email_from')}</field>
        <field name="email_to">${ctx.get('email_to')}</field>
        <field name="subject">Request For Approval</field>
        <field name="model_id" ref="equip3_accounting_operation.model_account_payment"/>
        <field name="auto_delete" eval="True"/>
        <field name="body_html"><![CDATA[
            <p>Date ${ctx.get('date')},</p>
            <p>Subject : Request for Approval of Receipt</p>
            <p>Dear ${ctx.get('approver_name')},</p>
            <p>You have approval pending that needs your decision to approve or reject the receipt.</p>
            <p>${object.request_partner_id.name} has requested a ${ctx.get('currency')} by  ${ctx.get('payment_journal')}.
            </p>
            <p>You can approve or reject the receipt by clicking this button:</p>
            <div style = "margin: 16px;">
                <a href=${ctx.get('url')} style = "padding: 5px 10px; font-size: 12px; line-height: 18px;
                 color: #FFFFFF; border-color:#875A7B; text-decoration: none; display: inline-block; 
                 margin-bottom: 0px; font-weight: 400; text-align: center; vertical-align: middle; 
                 cursor: pointer; white-space: nowrap; background-image: none; background-color: #875A7B;
                 border: 1px solid #875A7B; border-radius:3px">View Receipt</a></div>
            <p>Thank you,</p>
            <p>This is an automatically generated email, please do not reply.</p>
            ]]></field>
    </record>


    <record id="email_template_receipt_submitter_approval_matrix" model="mail.template">
        <field name="name">Submitter Approval Matrix</field>
        <field name="email_from">${ctx.get('email_from')}</field>
        <field name="email_to">${ctx.get('email_to')}</field>
        <field name="subject">Submitter Approval</field>
        <field name="model_id" ref="equip3_accounting_operation.model_account_payment"/>
        <field name="auto_delete" eval="True"/>
        <field name="body_html"><![CDATA[
            <p>Date ${ctx.get('date')},</p>
            <p>Subject : Approval of Receipt</p>
            <p>Dear ${object.request_partner_id.name}</p>
            <p>We have received a response to your request for an receipt on ${ctx.get('create_date')}.
            </p>
            <p>This notification serves as confirmation that your receipt has been approved.
            </p>
            <p>Thank you,</p>
            <p>This is an automatically generated email, please do not reply.</p>
            ]]></field>
    </record>

    <record id="email_template_receipt_rejected_matrix" model="mail.template">
        <field name="name">Rejected Approval Matrix</field>
        <field name="email_from">${ctx.get('email_from')}</field>
        <field name="email_to">${ctx.get('email_to')}</field>
        <field name="subject">Rejected Approval</field>
        <field name="model_id" ref="equip3_accounting_operation.model_account_payment"/>
        <field name="auto_delete" eval="True"/>
        <field name="body_html"><![CDATA[
            <p>Dear ${object.request_partner_id.name}</p>
            <p>We have received a response to your request for an receipt on ${ctx.get('create_date')}.
            </p>
            <p>This notification serves as confirmation that your receipt has been rejected.
            </p>
            <p>Here is the feedback from ${ctx.get('rejected_name')}</p>
            <p>${ctx.get('feedback')}</p>
            <p>Thank you,</p>
            <p>This is an automatically generated email, please do not reply.</p>
            ]]></field>
    </record>

</odoo>
