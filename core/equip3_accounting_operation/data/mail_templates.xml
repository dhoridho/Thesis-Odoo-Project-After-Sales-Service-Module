<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="email_template_bill_due_date_notification" model="mail.template">
        <field name="name">Bill Due Date Notification</field>
        <field name="email_from">${ctx.get('email_from')}</field>
        <field name="email_to">${ctx.get('email_to')}</field>
        <field name="subject">Reminder Bill on Due Date</field>
        <field name="model_id" ref="account.model_account_move"/>
        <field name="body_html"><![CDATA[
            <p>Date ${ctx.get('date')},</p>
            <p>Subject : Reminder Bill on Due Date,</p>
            <p>Dear ${ctx.get('user')},</p>
            <p>You have bill on due date, there was ${ctx.get('amount')} on branch ${ctx.get('branch')}.</p>
           <p>Click on this link if you want to check the bill(s)   </p>
             <p>${ctx.get('link')}</p>
            <p>Pay the bill before the due date to avoid the payment penalty.</p>
            <p>Thank you,</p>
            <p>This is an automatically generated email, please do not reply.</p>
            ]]></field>
    </record>

    <record id="email_template_bill_due_date_reminder_notification" model="mail.template">
        <field name="name">Bill Due Date Reminder Notification</field>
        <field name="email_from">${ctx.get('email_from')}</field>
        <field name="email_to">${ctx.get('email_to')}</field>
        <field name="subject">Reminder to Pay Bill after due date</field>
        <field name="model_id" ref="account.model_account_move"/>
        <field name="body_html"><![CDATA[
            <p>Date ${ctx.get('date')},</p>
            <p>Subject : Reminder to Pay Bill after due date,</p>
            <p>Dear ${ctx.get('user')},</p>
            <p>You have bill pass due date, there was  ${ctx.get('amount')} on branch ${ctx.get('branch')}.</p>
             <p>Click on this link if you want to check the bill(s)  </p>
             <p>${ctx.get('link')}</p>
            <p>Pay the bill that’s already past the due to avoid the payment penalty.</p>
            <p>Thank you,</p>
            <p>This is an automatically generated email, please do not reply.</p>
            ]]></field>
    </record>

    <record id="email_template_invoice_reminder_after_notification" model="mail.template">
        <field name="name">Reminder Invoice on Due Date</field>
        <field name="email_from">${ctx.get('email_from')}</field>
        <field name="email_to">${ctx.get('email_to')}</field>
        <field name="subject">Reminder Invoice on Due Date</field>
        <field name="model_id" ref="account.model_account_move"/>
        <field name="body_html"><![CDATA[
            <p>Date: ${ctx.get('date')}</p>
            <p>Subject: Reminder Invoice on Due Date</p>
            <p>Dear ${ctx.get('user')},</p>
            <p>You have an invoice past the due date, there was ${ctx.get('amount')} on branch ${ctx.get('branch')}.</p>
             <p>Clik on this link if you want to check the invoice(s) </p>
             <p>${ctx.get('link')}</p>
            <p>Remind the customer to pay the invoice that’s already past the due date to avoid more payment penalty.</p>
            <p>Thank you,</p>
            <p>This is an automatically generated email, please do not reply.</p>
            ]]></field>
    </record>

    <record id="email_template_invoice_reminder_before_notification" model="mail.template">
        <field name="name">Reminder Invoice on Due Date</field>
        <field name="email_from">${ctx.get('email_from')}</field>
        <field name="email_to">${ctx.get('email_to')}</field>
        <field name="subject">Reminder Invoice on Due Date</field>
        <field name="model_id" ref="account.model_account_move"/>
        <field name="body_html"><![CDATA[
            <p>Date: ${ctx.get('date')}</p>
            <p>Subject: Reminder Invoice on Due Date</p>
            <p>Dear ${ctx.get('user')},</p>
            <p>You have invoice on due date, there was ${ctx.get('amount')} on branch ${ctx.get('branch')}.</p>

            <p>Clik on this link if you want to check the invoice(s) </p>
            <p>${ctx.get('link')}</p>
            <p>Remind the customer to pay the invoice before the due date to avoid payment penalty.</p>
            <p>Thank you,</p>
            <p>This is an automatically generated email, please do not reply.</p>
            ]]></field>
    </record>

    <record id="email_template_invoice_followup_notification" model="mail.template">
        <field name="name">Follow up Invoice</field>
        <field name="subject">Reminder to Pay the Invoices</field>
        <field name="email_from">${ctx.get('email_from')}</field>
        <field name="email_to">${object.partner_id.email}</field>
        <field name="partner_to">${object.partner_id.id}</field>
        <field name="model_id" ref="equip3_accounting_operation.model_followup_invoice_tmp"/>
        <field name="body_html" type="html">
            <div style="margin: 0px; padding: 0px;">
                <p style="margin: 0px; padding: 0px; font-size: 13px;">
                    Date ${ctx.get('date')},<br/>
                    Subject : Reminder to Pay the Invoices,<br/>
                    ${ctx.get('overdue_template')}
                </p>
            </div>
        </field>
        <field name="report_template" ref="equip3_accounting_operation.action_report_statement"/>
        <field name="report_name">Reminder to Pay the Invoices</field>
        <field name="lang">${object.partner_id.lang}</field>
    </record>

    <record id="email_template_bill_approval_matrix" model="mail.template">
        <field name="name">Approval Bill</field>
        <field name="email_from">${ctx.get('email_from')}</field>
        <field name="email_to">${ctx.get('email_to')}</field>
        <field name="subject">Application for Bill Approval</field>
        <field name="model_id" ref="equip3_accounting_operation.model_account_move"/>
        <field name="auto_delete" eval="True"/>
        <field name="body_html"><![CDATA[
            <p>Date ${ctx.get('date')},</p>
            <p>Subject : Application for Bill Approval,</p>
            <p>Dear ${ctx.get('approver_name')},</p>
            <p>You have approval pending that needs your decision to approve or reject the bill.</p>
            <p>${object.request_partner_id.name} has requested a ${ctx.get('currency')} bill to ${object.partner_id.name} is due on ${ctx.get('due_date')} at ${ctx.get('date_invoice')}.
            </p>
            <p>You can approve or reject the bill by clicking this button:</p>
            <div style = "margin: 16px;">
                <a href=${ctx.get('url')} style = "padding: 5px 10px; font-size: 12px; line-height: 18px;
                 color: #FFFFFF; border-color:#875A7B; text-decoration: none; display: inline-block; 
                 margin-bottom: 0px; font-weight: 400; text-align: center; vertical-align: middle; 
                 cursor: pointer; white-space: nowrap; background-image: none; background-color: #875A7B;
                 border: 1px solid #875A7B; border-radius:3px">View Bill</a></div>
            <p>Thank you,</p>
            <p>This is an automatically generated email, please do not reply.</p>
            ]]></field>
    </record>

    <record id="email_template_submitter_bill_approval_matrix" model="mail.template">
        <field name="name">Submitter Approval Matrix Bill</field>
        <field name="email_from">${ctx.get('email_from')}</field>
        <field name="email_to">${ctx.get('email_to')}</field>
        <field name="subject">Submitter Approval Bill</field>
        <field name="model_id" ref="equip3_accounting_operation.model_account_move"/>
        <field name="auto_delete" eval="True"/>
        <field name="body_html"><![CDATA[
            <p>Date ${ctx.get('date')},</p>
            <p>Subject : Approval of  Bill</p>
            <p>Dear ${object.request_partner_id.name}</p>
            <p>We have received a response to your request for an  Bill on ${ctx.get('create_date')}.
            </p>
            <p>This notification serves as confirmation that your Bill has been approved.
            </p>
            <p>Thank you,</p>
            <p>This is an automatically generated email, please do not reply.</p>
            ]]></field>
    </record>

    <record id="email_template_rejected_bill_matrix" model="mail.template">
        <field name="name">Rejected Approval Bill</field>
        <field name="email_from">${ctx.get('email_from')}</field>
        <field name="email_to">${ctx.get('email_to')}</field>
        <field name="subject">Rejected Approval Bill</field>
        <field name="model_id" ref="equip3_accounting_operation.model_account_move"/>
        <field name="auto_delete" eval="True"/>
        <field name="body_html"><![CDATA[
            <p>Dear ${object.request_partner_id.name}</p>
            <p>We have received a response to your request for an Bill on ${ctx.get('create_date')}.
            </p>
            <p>This notification serves as confirmation that your Bill has been rejected.
            </p>
            <p>Here is the feedback from ${ctx.get('rejected_name')}</p>
            <p>${ctx.get('feedback')}</p>
            <p>Thank you,</p>
            <p>This is an automatically generated email, please do not reply.</p>
            ]]></field>
    </record>

</odoo>