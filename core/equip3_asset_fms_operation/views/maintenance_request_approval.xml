<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- approval.matrix.maintenance.request tree view -->
        <record id="approval_matrix_maintenance_request_view_tree" model="ir.ui.view">
            <field name="name">approval.matrix.maintenance.request.view.tree</field>
            <field name="model">approval.matrix.maintenance.request</field>
            <field name="arch" type="xml">
                <tree>
                    <field name="name"/>                    
                </tree>
            </field>
        </record>

        <!-- approval.matrix.maintenance.request.line form view -->
        <record id="approval_matrix_maintenance_request_view_form" model="ir.ui.view">
            <field name="name">approval.matrix.maintenance.request.view.form</field>
            <field name="model">approval.matrix.maintenance.request</field>
            <field name="arch" type="xml">
                <form>
                    <sheet>
                        <group>
                            <group>
                                <field name="name"/>
                                <field name="company_id"/>
                                <field name="branch_id"/>
                            </group>
                            <group>
                                <field name="access_type"/>
                                <field name="group_id" force_save="1" readonly="1" invisible="1"/>
                                <field name="approval_group_id" force_save="1" readonly="1"/>
                            </group>
                        </group>
                        <notebook>
                            <page string="Approval Line">
                                <field name="approval_matrix_maintenance_line">
                                    <tree editable="bottom">
                                        <field name="user_ids" widget="many2many_tags"/>
                                        <field name="minimum_approval"/>
                                    </tree>
                                </field>
                            </page>
                        </notebook>
                    </sheet>
                </form>
            </field>
        </record>
    </data>

    <!-- approval.matrix.maintenance.request action window -->
    <record id="maintenance_requset_approval_action" model="ir.actions.act_window">
        <field name="name">Maintenance Approval Request</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">approval.matrix.maintenance.request</field>
        <field name="view_mode">tree,form</field>
    </record>

    <!-- This Menu Item must have a parent and an action -->
    <menuitem id="maintenance_approval_request_menu_act" name="Maintenance Request Approval Matrix" parent="equip3_asset_fms_masterdata.menu_approval_matrix" action="maintenance_requset_approval_action" sequence="-1"/>
</odoo>