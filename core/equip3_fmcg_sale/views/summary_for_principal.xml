<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="action_menu_summary_for_principal_xlsx" model="ir.actions.server">
        <field name="name">Summary for Principal</field>
        <field name="model_id" ref="model_sale_order_line"/>
        <field name="state">code</field>
        <field name="binding_model_id" ref="model_sale_order_line"/>
        <field name="binding_type">action</field>
        <field name="binding_view_types">list</field>
        <field name="code">
            if records:
                action = model.summary_for_principal_xlsx_report(records)
        </field>
    </record>
</odoo>