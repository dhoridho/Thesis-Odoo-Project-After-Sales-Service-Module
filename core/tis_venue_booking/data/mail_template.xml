<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <record id="email_template_edi_booking_final_3" model="mail.template">
            <field name="name">Booking Confirmation: Send by email</field>
            <field name="model_id" ref="tis_venue_booking.model_booking_booking"/>
            <field name="email_from">${object.user_id.email_formatted |safe}</field>
            <field name="partner_to">${object.partner_id.id}</field>
            <field name="subject">(Ref ${object.name})</field>
            <field name="body_html" type="html">
<div style="margin: 0px; padding: 0px;">
    <p style="margin: 0px; padding: 0px; font-size: 13px;">
        % set doc_name = ' booking reference' if object.state in ('confirm')
        Dear ${object.partner_id.name}
        % if object.partner_id.parent_id:
            (${object.partner_id.parent_id.name})
        % endif
        <br /><br />
        Your
        ${doc_name} <strong>${object.name}</strong>
         for venue <strong>${object.venue_id.name}</strong>
         from ${(object.from_date)} to ${(object.to_date)} has been confirmed.
        <br /><br />
        Do not hesitate to contact us if you have any question.
    </p>
</div>
            </field>

        </record>
    </data>
</odoo>

