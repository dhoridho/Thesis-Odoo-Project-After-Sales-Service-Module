<?xml version='1.0' encoding='utf-8'?>
<odoo>
    <data noupdate="1">
    
        <record model="ir.cron" id="cron_remove_stock_picking_unique_name">
            <field name="name">Base Synchro: Remove Unique Name Constraint</field>
            <field name="model_id" ref="model_stock_picking" />
            <field name="state">code</field>
            <field name="code">model.remove_stock_picking_unique_name()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="active">False</field>
        </record>

        <record model="ir.cron" id="cron_sync_object">
            <field name="name">Sync Object</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="numbercall">-1</field>
            <field name="nextcall" eval="(DateTime.now().replace(hour=2, minute=0) + timedelta(days=1)).strftime('%Y-%m-%d %H:%M:%S')" />
            <field name="doall" eval="False"/>
            <field name="model_id" ref="model_base_synchro"/>
            <field name="code">model.running_scheduler()</field>
            <field name="state">code</field>
        </record>

    </data>
    
</odoo>