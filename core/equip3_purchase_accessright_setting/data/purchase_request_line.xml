<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="action_purchase_request_tender_line" model="ir.actions.server">
        <field name="name">Create Purchase Tender</field>
        <field name="type">ir.actions.server</field>
        <field name="state">code</field>
        <field name="model_id" ref="purchase_request.model_purchase_request_line"/>
        <field name="binding_model_id" ref="purchase_request.model_purchase_request_line"/>
        <field name="code">
            if records:
                    action = records.create_purchase_tender()
        </field>
    </record>

    <!-- <record id="action_purchase_request_blanket_order_line" model="ir.actions.server">
        <field name="name">Create Blanket Order</field>
        <field name="type">ir.actions.server</field>
        <field name="state">code</field>
        <field name="model_id" ref="purchase_request.model_purchase_request_line"/>
        <field name="binding_model_id" ref="purchase_request.model_purchase_request_line"/>
        <field name="code">
            if records:
                    action = records.create_blanket_order()
        </field>
    </record> -->

</odoo>
