<?xml version="1.0" encoding="utf-8"?>

<templates id="backend_views" xml:space="preserve">


    <t t-extend="KanbanView.buttons">
        <!-- Add responsive icons to buttons -->
        <t t-jquery="button:first" t-operation="before">
            <button type="button" class="btn btn-primary o_button_remote_session_pos" t-if="widget and widget.modelName and widget.modelName == 'pos.config'">
                Remote Session
            </button>
        </t>
    </t>

    <t t-extend="FormView.buttons">
        <!-- Add responsive icons to buttons -->
        <t t-jquery="button.o_form_button_save" t-operation="before">
            <button type="button" class="btn btn-primary o_button_remote_session_pos"  t-if="widget and widget.modelName and widget.modelName == 'pos.config'">
                Remote Session
            </button>
        </t>
    </t>


</templates>
