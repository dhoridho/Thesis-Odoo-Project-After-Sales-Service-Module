<?xml version="1.0"?>
<openerp>
    <data noupdate="0">
        <record id="email_template_maintenance_create_custom" model="mail.template">
            <field name="name">Maintenance Received Notification</field>
            <field name="model_id" ref="maintenance.model_maintenance_request"/>
            <field name="auto_delete" eval="True"/>
            <field name="email_from">${user.company_id.partner_id.email_from}</field>
            <field name="email_to">${object.owner_user_id.partner_id.email}</field>
            <field name="subject">Maintenance Request Opened: ${object.name}</field>
            <field name="body_html"><![CDATA[
            
<p>Dear ${object.owner_user_id.name},

<p>We have received your maintenance request. And our maintenance team shortly contact with you.

<p>Equipment: <b>${object.equipment_id.name}</b></p>
 <p>Submitted: <b>${object.request_date}</b></p>
           <br/>
           <tr>
        <td align="center" style="min-width: 590px;">
            <table border="0" cellpadding="0" cellspacing="0" width="590" style="min-width: 590px; background-color: white; font-size: 11px; padding: 0px 8px 0px 8px; border-collapse:separate;">
                <tr><td valign="middle" align="left">
                    ${user.company_id.name}
                </td></tr>
                <tr><td valign="middle" align="left" style="opacity: 0.7;">
                    ${user.company_id.phone or ''}
                    % if object.company_id.email
                        | <a href="'mailto:%s' % ${object.company_id.email}" style="text-decoration:none; color: #454748;">${user.company_id.email}</a>
                    % endif
                    % if object.company_id.website
                        | <a href="'%s' % ${object.company_id.website}" style="text-decoration:none; color: #454748;">
                        ${object.company_id.website}
                    </a>
                    % endif
                </td></tr>
            </table>
        </td>
    </tr>
            <tr><td align="center" style="min-width: 590px;">
                <table border="0" cellpadding="0" cellspacing="0" width="590" style="min-width: 590px; background-color: #F1F1F1; color: #454748; padding: 8px; border-collapse:separate;">
                  <tr><td style="text-align: center; font-size: 13px;">
                    Powered by <a target="_blank" href="https://www.hashmicro.com?utm_source=db&amp;utm_medium=portalinvite" style="color: #875A7B;">Odoo</a>
                  </td></tr>
                </table>
            </td></tr>
   ]]>
            <!-- <![CDATA[
                <p>Dear ${object.owner_user_id.name},</p>
                                        <p>We have received your maintenance request. And our maintenance team shortly contact with you.</p>
                                        <p>Equipment: <b>${object.equipment_id.name}</b></p>
                                        <p>Submitted: <b>${object.request_date}</b></p>
                                        <p>Your Company</p>
                                        <p>${user.company_id.name}<br/>
                                        <p>${user.company_id.phone or ''}</p>
                                        <p>
                                        % if user.company_id.email:
                                <a href="mailto:${user.company_id.email}" style="text-decoration:none; color: white;">${user.company_id.email}</a><br/>
                                % endif
                                % if user.company_id.website:
                                    <a href="${user.company_id.website}" style="text-decoration:none; color: white;">
                                        ${user.company_id.website}
                                    </a>
                                % endif
                                    </p>]]> -->
                                    
            </field>
        </record>

    </data>
</openerp>
