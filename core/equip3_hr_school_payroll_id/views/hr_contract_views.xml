<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="equip3_school_masterdata_inherit_hr_contract_view_form" model="ir.ui.view">
        <field name="name">hr.contract.form.inherit</field>
        <field name="model">hr.contract</field>
        <field name="inherit_id" ref="hr_contract.hr_contract_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='wage']" position="after">
                <label for="rate_per_hour"/>
                <div class="o_row" name="rate_per_hour">
                    <field name="rate_per_hour" nolabel="1"/>
                    <span>/ hour</span>
                </div>

                <label for="rate_per_class"/>
                <div class="o_row" name="rate_per_class">
                    <field name="rate_per_class" nolabel="1"/>
                    <span>/ classes</span>
                </div>
            </xpath>
        </field>
    </record>

</odoo>
