<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_mrp_approval_matrix_entry_tree" model="ir.ui.view">
        <field name="name">view.mrp.approval.matrix.entry.tree</field>
        <field name="model">mrp.approval.matrix.entry</field>
        <field name="arch" type="xml">
            <tree string="Approval Matrix Lines" default_order="sequence,id">
                <field name="line_id" invisible="1"/>
                <field name="line_ids" invisible="1"/>
                <field name="matrix_type" invisible="1"/>

                <field name="sequence"/>
                <field name="approver_ids" widget="many2many_tags"/>
                <field name="minimum_approver"/>
                <field name="description"/>
                
                <field name="requested_id" optional="hide"/>
                <field name="requested_time" optional="hide"/>
                <field name="approved_ids" widget="many2many_tags" optional="hide"/>
                <field name="rejected_ids" widget="many2many_tags" optional="hide"/>
                <field name="need_action_ids" widget="many2many_tags" optional="hide"/>
                <field name="state" widget="badge" decoration-success="state == 'approved'" decoration-danger="state == 'rejected'" optional="hide"/>
            </tree>
        </field>
    </record>

    <record id="view_mrp_approval_matrix_entry_form" model="ir.ui.view">
        <field name="name">view.mrp.approval.matrix.entry.form</field>
        <field name="model">mrp.approval.matrix.entry</field>
        <field name="arch" type="xml">
            <form string="Approval Matrix Lines">
                <header>
                    <field name="state" widget="statusbar" statusbar_visible="draft,to_approve,approved"/>
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="line_id" string="Matrix Line"/>
                            <field name="matrix_type"/>
                            <field name="requested_id"/>
                            <field name="requested_time"/>
                        </group>
                        <group>
                            <field name="sequence"/>
                            <field name="minimum_approver" string="Min. Approver"/>
                            <field name="approver_ids" widget="many2many_tags"/>
                            <field name="approved_ids" widget="many2many_tags"/>
                            <field name="rejected_ids" widget="many2many_tags"/>
                            <field name="need_action_ids" widget="many2many_tags"/>
                        </group>
                    </group>
                    <group string="Approval Status">
                        <field name="description" nolabel="1"/>
                    </group>
                    <notebook>
                        <page string="Approval Activity" name="line_ids">
                            <field name="line_ids">
                                <tree string="Approval Activity">
                                    <field name="entry_id" invisible="1"/>
                                    <field name="approver_id"/>
                                    <field name="action_time"/>
                                    <field name="note"/>
                                    <field name="state" widget="badge" decoration-success="state == 'approved'" decoration-danger="state == 'rejected'"/>
                                </tree>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_mrp_approval_matrix_entry_reason_tree" model="ir.ui.view">
        <field name="name">view.mrp.approval.matrix.entry.reason.tree</field>
        <field name="model">mrp.approval.matrix.entry.reason</field>
        <field name="arch" type="xml">
            <tree string="Reason Rejected">
                <field name="name"/>
            </tree>
        </field>
    </record>

    <record id="view_mrp_approval_matrix_entry_reason_form" model="ir.ui.view">
        <field name="name">view.mrp.approval.matrix.entry.reason.form</field>
        <field name="model">mrp.approval.matrix.entry.reason</field>
        <field name="arch" type="xml">
            <form string="Reason Rejected">
                <sheet>
                    <group>
                        <field name="name" class="oe_inline"/>
                    </group>                       
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_view_mrp_approval_matrix_entry_reason" model="ir.actions.act_window">
        <field name="name">Reason</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">mrp.approval.matrix.entry.reason</field>
        <field name="view_mode">tree,form</field>
        <field name="help">Create Reason(s)</field>
    </record>
</odoo>
